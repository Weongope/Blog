@model IEnumerable<Post>

@{
    ViewBag.Title = "Blog Website";
    ViewBag.Description = "This Blog Website has more information";
    ViewBag.Keywords = "Blog programming travel photography";
    var base_path = Context.Request.PathBase;
}

@*<h2>Home page! </h2>

    <img src="~/content/static/lotr.jpg" width="600"/>*@

<div class="main-img">
    <span class="title">Welcome to Hogwarts School of Witchcraft and Wizardry</span>
</div>

<div class="container">
    @*   <ul> 
            @foreach (var post in Model)
            {
                <li>
                    <a asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">
                        @post.Title
                    </a>

                </li>
            }
        </ul>*@

    @foreach (var post in Model)
    {
        var image_path = $"{base_path}/Image/{post.Image}";
        <a class="post" asp-controller="Home" asp-action="Post" asp-route-id="@post.Id">
            <img src="@image_path" />
            <span class="title"> @post.Title </span>
        </a>
    }
</div>